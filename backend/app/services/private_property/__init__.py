"""Private (non-HDB) residential property mode — URA transactions (Feature 2).

Adapter layer over the URA Private Residential Property Transactions API with a
bundled-fixture fallback so dev/CI work without credentials. See
docs/DATA_SOURCES.md.
"""
