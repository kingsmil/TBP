# Backend runtime + dev dependencies (install on your local machine).
# The pure-Python geospatial core has NO third-party runtime deps and is
# tested with the stdlib `unittest` runner, so it runs even before these
# are installed. The packages below power the production PostGIS/FastAPI path.

# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.13.4
pydantic-settings==2.7.0
pydantic-ai-slim[anthropic,openai]==1.106.0

# Database / PostGIS
SQLAlchemy==2.0.36
GeoAlchemy2==0.16.0
psycopg[binary]==3.2.3

# Caching / jobs
redis==5.2.1
celery==5.4.0

# Data processing
pandas==2.2.3
numpy==2.1.3
shapely==2.0.6

# Live data fetching (data.gov.sg + OneMap)
requests==2.32.3

# Dev / test
pytest==8.3.4
httpx==0.28.1
