# Loaded by Vite only during `vite dev` (never in production builds).
# Mirrors the backend AUTH_REQUIRED=false dev bypass: the frontend treats the
# visitor as a fake subscribed dev user, so no login/subscription gating shows.
VITE_AUTH_REQUIRED=false
